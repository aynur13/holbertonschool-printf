.TH PRINTF 3 "November 2024" "Holberton" "Library"
.SH NAME
printf \- formatted output conversion
.SH SYNOPSIS
.B #include <stdio.h>
.sp
.B int printf(const char *format, ...);
.SH DESCRIPTION
The \fBprintf\fR function prints formatted output to stdout. The format string can contain ordinary characters and format specifiers (preceded by '%'). The function takes a variable number of arguments corresponding to the format specifiers.

Supported format specifiers include:
.TP
.B %d
Prints an integer (decimal).
.TP
.B %c
Prints a single character.
.TP
.B %s
Prints a string.
.TP
.B %f
Prints a floating-point number.
.TP
.B %x
Prints an integer in hexadecimal.

.SH RETURN VALUE
Returns the number of characters printed, excluding the null byte, or -1 on error.

.SH EXAMPLES
.nf
  int num = 42;
  printf("Number: %d\n", num);
.fi
Output:
  Number: 42

.nf
  printf("Hello, %s!\n", "world");
.fi
Output:
  Hello, world!

.SH SEE ALSO
.BR printf (1), 
.BR sprintf (3)
